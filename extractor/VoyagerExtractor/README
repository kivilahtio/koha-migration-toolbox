Files in this directory are used to extract data from Voyager.
First, copy this directory to the Voyager DB server.
Then cd into this directory on Voyager DB server.

Then create ./config.pl and configure it with DB connection params:

$ cp config/config.pl .
$ $EDITOR config.pl

Finally run

$ ./extract.pl --bound -A -B -H --precision

to write .csv-files to the VoyagerDB-server

